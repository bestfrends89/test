apiVersion: v1
kind: Pod
metadata:
  name: kuard
  annotations:
    dev.okteto.com/auto-ingress: "private"
spec:
  containers:
    - image: gcr.io/kuar-demo/kuard-amd64:blue
      name: kuard
      resources:
        requests:
          cpu: "500m"
          memory: "128Mi"
      ports:
        - containerPort: 8080
          name: http
          protocol: TCP
